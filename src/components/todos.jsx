import { Button } from "bootstrap"

const Todos = ({id, name, value})=>{
    return(
        <div>
            <input type={checkout} />
            <p>{name}</p>
            <p>{value}</p>
            <p>{value}</p>
            <p>{value}</p>
            
            <Button variant="primary">Primary</Button>
        </div>
    )
}
export default Todos